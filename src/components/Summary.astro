---
import type { SummaryItems } from "@myTypes/typesProps";

interface Props {
    summary: SummaryItems[];
}

const { summary } = Astro.props;
// console.log(summary);
---

<ul class="mt-4 space-y-2">
    {
        summary.map((item) => (
            <li
                class:list={[
                    "text-mocha-900 dark:text-mocha-100 hover:text-mocha-500 hover:underline w-fit font-medium",
                    item.component === "title_three" &&
                        "text-sm pl-3 opacity-65",
                ]}
            >
                <a href={`#${item.slug}`}>{item.label}</a>
            </li>
        ))
    }
</ul>
